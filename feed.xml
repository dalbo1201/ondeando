---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <link href="{{ "/feed.xml" | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ "/" | absolute_url }}" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ "/" | absolute_url }}</id>
  <author>
    <name>{{ site.author | xml_escape }}</name>
  </author>

  {% assign all_posts = site.blog | concat: site.fotolog | sort: "date" | reverse %}
  {% for post in all_posts limit: 10 %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url }}">{{ post.content | xml_escape }}</content>
    <author>
      <name>{{ site.author | xml_escape }}</name>
    </author>
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}"/>
    {% endfor %}
    {% if post.image %}
    <media:thumbnail url="{{ post.image | absolute_url }}"/>
    <media:content medium="image" url="{{ post.image | absolute_url }}"/>
    {% endif %}
  </entry>
  {% endfor %}
</feed>